name: 🚀 Deploy File to c `npm install` step more gracefully.

### Updated WorkflowPanel
on:
  push:
    branches:
      - main
jobs:
Here is an updated version of the workflow  FTP-Deploy-Action:
    name: 🎉 Deploy
    runs-on: ubuntu-24.04

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: Use Node.js to improve error handling and ensure successful file 16
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Cache npm dependencies
        uses: actions/cache generation:

```yaml
name: 🚀 Deploy File to cPanel@v3
        id: cache-node-modules
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}

      - name: 🔨 Install Dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: |
          npm install || { echo "npm install failed"; exit 1; }
          git reset --hard

     
on:
  push:
    branches:
      - main
jobs:
  FTP-Deploy-Action:
    name: 🎉 Deploy
    runs-on: ubuntu - name: 📂 Build Project
        run: npm run build || { echo "npm run build failed"; exit 1; }

      --24.04

    steps:
      - name: 🚚 Get latest code name: 📂 Sync folders and files to the server
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password
        uses: actions/checkout@v3
        with:
         : ${{ secrets.FTP_PASSWORD }}
